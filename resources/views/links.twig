{% import options.get('macro', 'anomaly.module.navigation::macro') as macro %}

<ul class="{{ options.list_class }}">
    {{ macro.links(options.parent ? links.children(options.parent) : links.top(), links, options, 1) }}
</ul>
