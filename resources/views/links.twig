{% import options.macro ?: "anomaly.module.navigation::macro" as macro %}

<ul class="{{ options.list_class }}">
    {{ macro.links(options.parent ? options.parent.children : links.root(), links, options) }}
</ul>
